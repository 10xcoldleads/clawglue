{
  "tools": [
    {
      "id": "security-check",
      "name": "Security Check",
      "description": "Verify firewall and SSH lockdown status",
      "version": "1.0",
      "detect_command": "test -f /usr/local/bin/security-check"
    }
  ]
}
